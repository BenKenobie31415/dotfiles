#################################
#             Fixes             #
#################################
fading = true;
# fade-delta = 20;
# fade-in-step = 1;
# fade-out-step = 1;

#################################
#     Background-Blurring       #
#################################
blur: {
  method = "dual_kawase";
  strength = 11;
}

blur-background = false;
blur-background-exclude = "(!class_g = 'st-256color' && !class_i = 'presel_feedback')";
opacity-rule = [ "64:class_g = 'Bspwm' && class_i = 'presel_feedback'" ];

# Make transparent windows clip other windows like non-transparent windows do,
# instead of blending on top of them.
#
transparent-clipping = true

# Specify a list of conditions of windows that should never have transparent
# clipping applied. Useful for screenshot tools, where you need to be able to
# see through transparent parts of the window.
#
transparent-clipping-exclude = ["(!class_g = 'st-256color')"]

#################################
#       General Settings        #
#################################
backend = "glx";
vsync = true;

# Use X Sync fence to sync clients' draw calls, to make sure all draw
# calls are finished before picom starts drawing. Needed on nvidia-drivers
# with GLX backend for some users.
#
xrender-sync-fence = true

#################################
#          Performance          #
#################################

# GLX backend: Avoid using stencil buffer, useful if you don't have a stencil buffer.
# Might cause incorrect opacity when rendering transparent content (but never
# practically happened) and may not work with blur-background.
# My tests show a 15% performance boost. Recommended.
#
glx-no-stencil = true;

# GLX backend: Avoid rebinding pixmap on window damage.
# Probably could improve performance on rapid window content changes,
# but is known to break things on some drivers (LLVMpipe, xf86-video-intel, etc.).
# Recommended if it works.
#
glx-no-rebind-pixmap = true


# wintypes:
# {
#   tooltip = { fade = false; shadow = false; opacity = 1.0; focus = true; full-shadow = false; }
#   dock = { shadow = false; clip-shadow-above = false; full-shadow = false;}
#   dnd = { shadow = false; full-shadow = false;}
#   popup_menu = { opacity = 1.0; full-shadow = false;}
#   dropdown_menu = { opacity = 1.0; full-shadow = false;}
#   unknown = { shadow = false; full-shadow = false;}
#   desktop = { shadow = false; full-shadow = false;}
#   toolbar = { shadow = false; full-shadow = false;}
#   menu = { shadow = false; full-shadow = false;}
#   utility = { shadow = false; full-shadow = false;}
#   splash = { shadow = false; full-shadow = false;}
#   dialog = { shadow = false; full-shadow = false;}
#   normal = { shadow = false; full-shadow = false;}
#   notification = { shadow = false; full-shadow = false;}
#   combo = { shadow = false;}
# };
